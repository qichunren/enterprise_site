<%= form_for([:syspanel, @news]) do |f| %>
  <% if @news.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@navmenu.errors.count, "error") %> prohibited this navmenu from being saved:</h2>

      <ul>
      <% @navmenu.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  
  <div class="actions">
    <%= f.submit %> or <%= link_to 'Back', syspanel_news_index_path %>
  </div>
<% end %>
