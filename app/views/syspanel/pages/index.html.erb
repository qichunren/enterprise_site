<% title "网站页面管理" %>
<h2><%= Page.model_name.human %></h2>

<table class="list">
  <tr>             
    <th>页页ID</th>  
    <th>页页标题</th>
    <th>URL</th>
    <th>添加人</th>
    <th>是否发布</th>
    <th>操作</th>
  </tr>

<% @pages.each do |page| %>
  <tr>
    <td align="center"><%= page.id %></td> 
    <td><%= page.title %></td>
    <td><%= page.url_slug %></td>
    <td align="right"><%= page.created_by %></td>
    <td><%= page.is_published? ? "是" : "否" %></td>
    <td align="center"><%= link_to 'Show', [:syspanel, page] %> － <%= link_to 'Edit', edit_syspanel_page_path(page) %> － <%= link_to 'Destroy', [:syspanel, page], :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to '创建新页面', new_syspanel_page_path %>
